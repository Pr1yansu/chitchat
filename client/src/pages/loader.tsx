import Logo from "@/components/logo";

const Loader = () => {
  return (
    <div className="flex justify-center items-center h-screen w-screen">
      <Logo loading text="Chit chat" />
    </div>
  );
};

export default Loader;
